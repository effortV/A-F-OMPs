# OMPs only. Do not copy NF_* settings here.
# Paste these values into Streamlit Cloud -> App settings -> Secrets.

SILICONFLOW_API_KEY = "粘贴你的 SiliconFlow API 密钥"
SILICONFLOW_BASE_URL = "https://api.siliconflow.cn/v1"

LLM_MODEL = "deepseek-ai/DeepSeek-V4-Pro"
LIT_PREPROCESS_MODEL = "Pro/deepseek-ai/DeepSeek-V3.2"
EMBED_MODEL = "BAAI/bge-m3"

# Optional. Keep empty to retry the primary model without silent model changes.
LLM_FALLBACK_MODELS = ""

# Persistent OMPs server bridge. These three values are independent of NF.
OMPS_SERVER_APP_URL = "https://omps.112-15-87-51.sslip.io"
OMPS_BACKEND_URL = "https://omps.112-15-87-51.sslip.io/bridge"
OMPS_API_TOKEN = "由部署脚本生成的随机令牌"
